# /root/.hermes/whispera-hooks.env (chmod 0600)
POLL_TOKEN=<value of the worker's POLL_TOKEN secret>
WORKER_URL=https://whispera-agent-hooks.<subdomain>.workers.dev
# Optional overrides:
# HERMES_URL=http://127.0.0.1:8644/webhooks/whispera-pr-assistant
# INTERVAL=30
